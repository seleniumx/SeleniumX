package org.seleniumx.util;

public class Set {
    public enum OS {
        WINDOWS, LINUX, MAC
    }

    public enum WINDOW_SIZE {
        FULLSCREEN, MAXIMIZE, DEFAULT
    }
    public enum BROWSER{
        CHROME, FIREFOX, IE
    }
}